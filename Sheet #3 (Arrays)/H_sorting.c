#include<stdio.h>
int main()
{
    int i,n;
    scanf("%d", &n);
    int arr[n];
    for(i=0;i<n;i++){
        scanf("%d", &arr[i]);z
    }
    for(i=0,j=0;i<n,j<n;i++,j++){
        if(arr)
    }
    return 0;
}